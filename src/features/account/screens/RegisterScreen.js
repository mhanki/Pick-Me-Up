import { AccountBackground, AccountCover } from "../components/Account.styles";

export const RegisterScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
    </AccountBackground>
  )
};